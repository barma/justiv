'use strict';

import React from 'react';

const NotFoundPage = (props) => {
  return (<div className="container text-center">
    <h2 className="not-found-header">The Page you are looking for can't be found!</h2>
  </div>);
};

export default NotFoundPage;
